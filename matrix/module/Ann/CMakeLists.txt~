CMAKE_MINIMUM_REQUIRED( VERSION 2.5 )



PROJECT( Ann )




AUX_SOURCE_DIRECTORY( . DIR_MAIN_SRC )

AUX_SOURCE_DIRECTORY( src DIR_ANN_SRC )

AUX_SOURCE_DIRECTORY( ../../matrix/ DIR_MATRIX_SRC )

INCLUDE_DIRECTORIES( include ../../matrix/ . )

FILE(GLOB_RECURSE ANN_HEADERS *.hpp)
SOURCE_GROUP("Include" FILES ${ANN_HEADERS} ) 

#LINK_DIRECTORIES( ../../build/Debug/ )
#LINK_DIRECTORIES( ../../build/Release/ )

ADD_LIBRARY( 03Ann ${DIR_ANN_SRC} ${DIR_MATRIX_SRC} ${DIR_MAIN_SRC} ${ANN_HEADERS} ../../Matrix.h )
ADD_EXECUTABLE( 02Ann ${DIR_ANN_SRC} ${DIR_MATRIX_SRC} ${DIR_MAIN_SRC} ${ANN_HEADERS} ../../Matrix.h )

#TARGET_LINK_LIBRARIES( 02Ann 01matrix.lib )
